export interface CartItem {
    name: string;
    quantity: number;
    price: number;
}

export interface Product {
    name: string;
    price: number;
    description: string;
    imgSrc: string;
}

